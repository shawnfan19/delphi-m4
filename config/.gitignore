# Ignore everything in the repo root (files and folders)
*
# …but keep all directories, so their contents are not ignored
!*/
# (optional) still track some root‑level files:
!.gitignore
!README.md
# Track panel configs
!panel/*.yaml
